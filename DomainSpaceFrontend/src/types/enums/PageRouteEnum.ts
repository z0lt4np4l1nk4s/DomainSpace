export enum PageRouteEnum {
  Home = "/",
  Login = "/login",
  Register = "/register",
  Account = "/account",
  Subjects = "/subjects",
  Users = "/users",
  PublishContent = "/content/publish",
  EditContent = "/content/edit/:id",
  ContentDetails = "/content/details/:id",
  ConfirmEmail = "/confirm-email/:token",
  ResetPassword = "/reset-password/:token",
  ForgotPassword = "/forgot-password",
}
